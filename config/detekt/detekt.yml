# Detekt configuration
# This file builds upon the default detekt configuration (buildUponDefaultConfig = true).
# Only overrides are specified here.
# Full reference: https://detekt.dev/docs/rules/style

complexity:
  LongMethod:
    maxLines: 60
  LongParameterList:
    maxParameters: 10
  TooManyFunctions:
    maxInFiles: 15
    maxInClasses: 15
    maxInInterfaces: 15
    maxInObjects: 15
    maxInEnums: 15

style:
  MaxLineLength:
    maxLineLength: 120
    excludeCommentStatements: true
  MagicNumber:
    ignorePropertyDeclaration: true
    ignoreAnnotation: true
    ignoreEnums: true
    ignoreNumbers:
      - '-1'
      - '0'
      - '1'
      - '2'
  WildcardImport:
    active: true
  ReturnCount:
    max: 4

naming:
  FunctionNaming:
    # Allow Compose @Composable functions to start with uppercase
    functionPattern: '[a-zA-Z][a-zA-Z0-9]*'

coroutines:
  active: true
